[project]
name = "jelq-kodi-repository"
version = "0.0.0"
requires-python = ">=3.8"
dependencies = []

[dependency-groups]
dev = [
    "kodi-addon-checker==0.0.36",
    "pytest==8.3.5",
    "ruff==0.16.7",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
